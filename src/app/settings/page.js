import Setting from "./setting";

export const metadata = {
  title: "หน้าตั้งค่าระบบ",
  description: "ตั้งค่าการแสดงผล ตัวแปรต่างๆ",
};

export default function Settings() {
  return <Setting />;
}
